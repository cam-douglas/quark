[pytest]
addopts = -ra -q -m "not legacy"
# Directories containing Quark test suites
testpaths =
    tests/smoke

# Ignore vendor code
norecursedirs = external* data* logs* management* audit_outputs* utilities/tools_utilities/testing_frameworks* tests/visualizations*

markers =
    legacy: deprecated or external tests ignored during reorg validation
