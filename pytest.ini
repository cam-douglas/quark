[pytest]
testpaths = tests
norecursedirs = data backups venv .venv docs repo_indexes
addopts = -q --disable-warnings --durations=10 -m "not heavy"
markers =
    heavy: marks tests as heavy/slow or requiring large local assets (deselect with '-m "not heavy"')


